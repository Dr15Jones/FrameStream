nocap#if !defined(PACKAGE_SKELNAME_H)
nocap#define PACKAGE_SKELNAME_H
nocap// -*- C++ -*-
nocap//
nocap// Package:     <package>
nocap// Module:      skelname
nocap// 
nocap/**\class skelname skelname.h package/skelname.h
nocap
nocap Description: <one line class summary>
nocap
nocap Usage:
nocap    <usage>
nocap
nocap*/
nocap//
nocap// Author:      John Doe
nocap// Created:     day-mon-xx
nocap// RCS(Id)
nocap//
nocap// Revision history
nocap//
nocap// RCS(Log)
nocap
nocap// system include files
nocap
nocap// user include files
nocap
nocap// forward declarations
nocap
nocapclass skelname
nocap{
nocap      // ---------- friend classes and functions ---------------
nocap
nocap   public:
nocap      // ---------- constants, enums and typedefs --------------
nocap
nocap      // ---------- Constructors and destructor ----------------
nocap      skelname();
nocap      virtual ~skelname();
nocap
nocap      // ---------- member functions ---------------------------
nocap
nocap      // ---------- const member functions ---------------------
nocap
nocap      // ---------- static member functions --------------------
nocap
nocap   protected:
nocap      // ---------- protected member functions -----------------
nocap
nocap      // ---------- protected const member functions -----------
nocap
nocap   private:
nocap      // ---------- Constructors and destructor ----------------
nocap      skelname( const skelname& ); // stop default
nocap
nocap      // ---------- assignment operator(s) ---------------------
nocap      const skelname& operator=( const skelname& ); // stop default
nocap
nocap      // ---------- private member functions -------------------
nocap
nocap      // ---------- private const member functions -------------
nocap
nocap      // ---------- data members -------------------------------
nocap
nocap      // ---------- static data members ------------------------
nocap
nocap};
nocap
nocap// inline function definitions
nocap
nocap// Uncomment the following lines, if your class is templated 
nocap// and has an implementation file (in the Template directory)
nocap//#if defined(INCLUDE_TEMPLATE_DEFINITIONS)
nocap//# include "package/Template/skelname.cc"
nocap//#endif
nocap
nocap#endif /* PACKAGE_SKELNAME_H */
